Agent 是指让大语言模型具备自主决策和工具调用能力的系统。

一个典型的 Agent 循环(比如 ReAct 模式)会让模型先思考(Thought),再决定是否调用某个工具(Action),拿到工具返回结果(Observation)之后再继续思考,直到得出最终答案。

Agent 常见的工具包括搜索引擎、计算器、数据库查询、代码执行环境等。设计 Agent 系统时,工具的描述是否清晰、返回结果是否结构化,直接影响模型调用工具的准确率。
