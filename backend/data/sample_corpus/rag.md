RAG(检索增强生成)是一种把外部检索系统和大语言模型结合起来的方法。

它的基本流程是:先根据用户的问题去一个知识库里检索相关文档片段,再把这些片段拼进 prompt 里,让大模型基于这些上下文生成回答。这样可以缓解大模型幻觉问题,也能让模型回答训练数据之外的最新知识。

常见的检索方式包括关键词检索(比如 BM25)和向量检索(基于 embedding 的语义相似度)。工程上通常会把两种召回方式结合起来,再做一次融合排序。
